# VistaBlox API

API-only backend for VistaBlox. This repository is being implemented from the architecture record in `../Docs/Backend`; when code and those documents disagree, the current design record and `03-Database/CORE_TABLES.md` take precedence.

## Implemented foundation

- Express 5 and TypeScript API runtime
- Zod validation for requests, responses, environment variables, and errors
- stable error envelope with a trace ID
- structured, redacted JSON logging
- liveness and database-readiness endpoints
- first layered product slice: public offering teasers at `GET /v1/offerings`
- authenticated full offering detail at `GET /v1/offerings/:offering_id`, including the current disclosure pack, material-change log, raise progress, and account-specific investment readiness
- authenticated disclosure downloads streamed through the API from private MinIO storage; object references and presigned storage URLs are never exposed
- self-hosted Better Auth core for email/password and optional Google login
- Better Auth's Expo plugin, trusting the native app's custom URI scheme (and, outside production, the Expo dev client's `exp://` scheme) so `vistablox-mobile` calls `/api/auth/*` directly for registration and sign-in — no browser or webview involved
- verified-email delivery through provider-neutral SMTP
- SMTP password-reset delivery with post-reset session revocation
- HIBP k-anonymity breached-password screening on password creation and changes
- stable Better Auth identity to VistaBlox `account_id` provisioning
- authenticated account-context middleware with fail-closed account restrictions
- mandatory per-session WebAuthn for staff/partner internal API access
- direct SimpleWebAuthn registration/authentication with multiple credential support
- one-time, session-bound WebAuthn challenges and success/failure audit records
- WebAuthn-protected, invitation-only staff/partner provisioning with scoped roles
- 72-hour single-use staff invitation tokens stored only as SHA-256 hashes
- idempotent Better Auth audit hooks for login, session, and password events
- privacy-preserving failed-login correlation without storing raw login identifiers
- WebAuthn-protected staff recovery that revokes sessions and credentials before reset delivery
- immediate staff/partner offboarding with Better Auth login blocking, role revocation, and audit logging
- staff/partner roster, incremental role grants, and single-role revocation at `GET /internal/v1/auth/staff-accounts`, `POST /internal/v1/auth/staff-accounts/:account_id/roles`, and `POST /internal/v1/auth/staff-accounts/:account_id/roles/:assignment_id/revoke` — the RBAC-administration gaps the invitation/offboarding pair above didn't cover: discovering existing `account_id`s and their role assignments at all (the roster's per-assignment `revoked_at` means it doubles as the role-history view, rather than needing a separate query), extending an already-onboarded staff member with an additional role without re-running the brand-new-identity invitation ceremony (`assertEmailAvailable` still blocks that ceremony for an existing identity, by design), and revoking one specific role without the full session/WebAuthn/status teardown `/offboard` performs. All three reuse the exact `admin_operations` + staff-WebAuthn gate and the same self-service block already established by recovery/offboarding — granting or revoking your own role is refused exactly like recovering or offboarding yourself is, so one compromised or careless admin session can't self-escalate or self-lock-out the roster. Granting a role the account already actively holds is a `409`, not a silent duplicate row. The roster is deliberately unpaginated, unlike every customer-activity list elsewhere in this API: staff/partner headcount is organizationally bounded, not a growth metric
- Didit v3 hosted individual-KYC session creation with opaque account correlation
- HMAC-SHA256 V2 authenticated Didit webhooks with timestamp replay protection and event idempotency
- webhook-then-fetch KYC decisions covering ID, liveness, face match, AML, age, and EU/EEA jurisdiction policy
- separate hosted Didit proof-of-address workflow with residence matching and three-month document freshness
- risk-aware baseline-KYC renewal intervals per `KYC_WORKFLOW.md`'s Renewal Policy: 24 months for a low-risk account, 12 for one that has ever resolved to `kyc_manual_review` (`identity.kyc_eligibility.ever_required_manual_review`, latched permanently the first time that happens — a past manual-review outcome is a phase-1 risk marker, not a rolling window — and still true if a later decision clears the account to `kyc_verified`); the existing renewal-reminder/expiry worker needed no changes since it already just reads whatever `renewal_due_at` was last computed
- privacy-minimized KYC persistence that excludes provider payloads, document data, biometrics, and addresses
- WebAuthn-protected operations decision display at `GET /internal/v1/kyc-accounts/:account_id`: the same `admin_operations` + staff-WebAuthn gate as founder origination review, reading the same already-persisted, already privacy-minimized eligibility record — never the raw Didit artifacts, which were never persisted in the first place. Surfaces the operational detail the customer's own `GET /v1/kyc` intentionally omits (`operational_substatus`, the Didit provider reference, declared residence/tax-residence countries, `ever_required_manual_review`) so a reviewer can actually see why an account is stuck in `pending_manual_review` or similar and why its renewal clock is set the way it is, closing the first of `docs/didit-kyc.md`'s follow-on-work gaps; a missing record reports `404` rather than a misleading default, since an arbitrary staff-supplied `account_id` might just be a typo
- authenticated investor profile aggregate at `GET /v1/investor-profile`
- wallet registration at `POST /v1/investor-profile/wallet` (`AD-241`'s DB-recording half: KYC-eligible, idempotent by account, rejects an address change or an address already claimed elsewhere as a `409`) — closes the gap where the profile's own `payment_account_ready`/`wallet` readiness fields could never become true because nothing ever wrote a `settlement.wallet_registrations` row. Deliberately does not prepare on-chain registration-transaction calldata or observe its confirmation (`registered_at` stays `null`): `AD-234` keeps production blockchain settlement dormant pending a written-counsel memo, and reservation creation's own readiness check only needs the row to exist, not on-chain confirmation. See [`docs/investor-profile.md`](docs/investor-profile.md)
- reservation creation at `POST /v1/offerings/:offering_id/reservations` (`AD-146`, `AD-255`): the capacity check and its write happen inside one transaction under a row lock on the `offerings` row, re-verifying both offering status and remaining capacity against fresh data rather than trusting the advisory blocker check `GET /v1/offerings/:offering_id` already exposes — the two share one rule set (`domain/reservation-eligibility.policy.ts`) so they can't disagree. A Coinbase CDP onramp session is created synchronously in the same request, the same pattern `StartKycSessionService` already uses for Didit (`AD-145` governs cross-domain state transitions, not a single external call whose purpose is handing the client a URL). The funding rail still defaults closed pending the expiry sweep and status poller below, so this endpoint exists but is not yet reachable end to end — see [`docs/investor-offering.md`](docs/investor-offering.md#reservation-creation)
- a Coinbase CDP onramp REST client (`HttpCoinbaseCdpClient`) using per-request JWT bearer auth verified directly against `@coinbase/cdp-sdk`'s own source rather than assumed (EdDSA/ES256 auto-detected from the configured key's shape); signs with `jose` directly instead of depending on the full `@coinbase/cdp-sdk` package, which pulls in an unrelated Solana/viem/axios dependency tree. See [`docs/coinbase-cdp-onramp.md`](docs/coinbase-cdp-onramp.md)
- the unfunded-reservation auto-expiry half of that same capacity-hold policy: `case_timers.reservation_unfunded_expiry` (`ExpireUnfundedReservationsService`, mirroring `ExpireOverdueInformationRequestsService`'s list-then-conditionally-act shape exactly) runs every minute and flips any `initiated` reservation older than 15 minutes to `lapsed`; capacity has no separate release step since `reserved_capacity_eur` already excludes `cancelled`/`lapsed` reservations at read/check time, so the stage flip alone frees the held capacity for other investors. Never expires a reservation whose `capital_state` already shows funded, however old it is — `reservation_stage` and `capital_state` are separate state machines (`AD-253`), and an `initiated`-stage reservation with money already attached is a normal in-between state, not a stale hold
- `case_timers.reservation_onramp_poll` (`PollOnrampTransactionsService`): the only way `capital_state` advances past `eurc_purchase_pending`, since Coinbase's onramp/offramp REST API has no webhook (confirmed against CDP's own API reference). Lists reservations still `eurc_purchase_pending`, polls each one's transaction history by reservation ID, and records `eurc_reserved` on a success or `purchase_failed` once nothing is still in flight — a success always wins over an earlier failed attempt on the same reservation. Runs every minute; unlike every other scheduled job in this worker, it is skipped entirely (not registered, scheduled, or worked) when Coinbase credentials aren't configured, since it is the first job whose sole dependency is genuinely optional. A purchase that succeeds after its reservation has already lapsed is a named, deliberately unresolved race — reconciling it is a policy question (in `AD-251`'s no-refund territory), not an implementation gap
- cursor-paginated investor reservation history and current portfolio subresources
- Redis/Valkey-backed, 24-hour protected cache for Didit-verified display names
- KYC/proof-of-address-gated owner intake at `POST /v1/origination-cases`
- owner-scoped origination case list/detail reads with opaque cursor pagination
- append-only initial submission revisions with mandatory evidence and audit logging
- founder-only origination review surface secured by active `admin_operations` assignment
- structured information requests, owner resubmission as immutable revision N+1, and atomic request resolution
- founder approval/rejection with review record, IPO terms, lifecycle transition, and audit logging
- founder-initiated case closure (withdrawal or a late-stage reject) at `POST /internal/v1/origination-cases/:case_id/close`, distinct from the submitted-stage initial review above: `PERMISSION_MATRIX.md`'s "Reject, withdraw, or expire a case, at any stage" row, matched against `REAL_ESTATE_INTAKE_LIFECYCLE.md`'s state diagram rather than that row's own looser wording — withdrawal is reachable from `draft`/`submitted`/`waiting_on_applicant`/`pre_offering_open` (conceptually the applicant's own choice per `AD-104`, but recorded by the founder, since the applicant-facing lane stays founder-mediated in phase 1), a late-stage reject only from `pre_offering_open` ("ipo_period ends underfunded, founder closes the case"). Automatic expiry (`waiting_on_applicant` with no response inside the SLA) already existed before this and needed no changes
- case discussion threads on the two human-postable lanes `REAL_ESTATE_INTAKE_LIFECYCLE.md`'s Thread Rules fix (`AD-244`'s 2026-08-29 update, which removed the earlier legal/appraisal workstream lanes entirely — no partner organizations to hold them in phase 1): `internal_case` (founder-only, matching `PERMISSION_MATRIX.md`'s "Post in internal case thread") and `applicant` (owner and founder, matching "Post in applicant thread"); the third lane, `system_timeline`, is system-generated and append-only per those same rules, so nothing here writes to it. The owner-facing routes are lane-implicit — `applicant` is the only lane the owner can ever reach, so there's no `lane` parameter to get wrong; the operations-facing ones take an explicit `lane`. A thread is created lazily on its first message rather than provisioned per case up front; `case_threads` gained a `(case_id, lane)` uniqueness constraint it was missing since the tables were first migrated, to actually guarantee the "same fixed lane set" rule now that something writes to them
- Redis/Valkey-backed general-purpose rate limiting across `/v1`, `/internal/v1`, and Better Auth's endpoints: a generous baseline tier keyed by `account_id` (or caller IP when unauthenticated), and a stricter tightened tier on fresh-auth attempts (Better Auth sign-in/sign-up/password-reset, staff WebAuthn ceremonies, staff invitation acceptance); fails open with a logged warning if the rate-limit cache is unavailable
- a dedicated `pg-boss` worker process (`npm run worker:dev` / `npm run worker:start`) running every scheduled job and the Didit webhook consumer: `case_timers` (applicant response-window reminders and automatic expiry once `due_at` passes), the KYC renewal-reminder timer and the automatic `requires_renewal` transition once `renewal_due_at` passes, stuck-session expiry/reconciliation, and `provider_events.didit_webhook` (`ProcessDiditWebhookService`, the durably-enqueued consumer side of the webhook the API's own `POST /webhooks/didit` receives, verifies, and durably enqueues)
- origination's, offering's, and investor-profile's own KYC-eligibility gating reads `identity.kyc_eligibility` directly, through the same shared `PrismaKycRepository` instance `/v1/kyc` itself uses (a standalone KYC microservice — its own process, reached over an internal signed HTTP call, with its own event-driven local copy of this table per consumer kept in sync by pg-boss publish/subscribe plus a periodic reconcile pass — was built and run here first; it was removed once the added indirection turned out not to be worth it for a system at this scale, with every consumer going back to a direct read against the shared table)
- the first exercised cross-domain `pg-boss` handoff (`AD-145`, "never a direct synchronous call"): a shared `enqueueTransactionalJob` helper (`src/shared/jobs/enqueue-job.ts`) is now the one path every cross-domain job send goes through — it auto-injects `trace_id` (`AD-152`, so no call site threads it through manually) and binds the send to the caller's own Prisma transaction via `pg-boss`'s per-call `db` adapter, so the job is durably queued in the exact same transaction as the state change that created it, never a separate step that could fail independently of it. `case_timers.pre_offering_open_handoff` is its first consumer: approving an origination case (`pre_offering_open`) enqueues a job that opens the `Piv`/`Offering` shell — `minimum_raise_eur` and `target_raise_eur` both set to the founder's own `ipo_value_eur`, `AD-238`/`AD-245`'s hard floor having collapsed the two into one number — that the property needs to actually become the browsable, reservation-ready listing `REAL_ESTATE_INTAKE_LIFECYCLE.md`'s "Property pre-offering" describes; before this, an approved case could never actually appear at `GET /v1/offerings`, since nothing wrote to that table at all. Idempotent by construction, not by an extra guard: `pivs.property_id`'s own uniqueness means replaying an already-processed job (a pg-boss retry, or the worker fetching the same job twice) is a safe no-op that returns the existing ids rather than a second row. This closes only the bridge from approval to first visibility — `PIV` incorporation, appraisal, corridor clearance/`AIF`-gating, and the later locked-terms `final_offering_published_at` publication described in the same document are separate, still-unbuilt follow-on work
- a general-purpose `Idempotency-Key` middleware (`createIdempotencyMiddleware`, `src/shared/http/idempotency.ts`) against the shared `audit.idempotency_keys` table: a replayed key with a matching request body returns the cached response without re-running the handler, a matching key with a different body is a `409`, and a lookup failure before the handler runs fails closed (`503`) while a caching failure after it fails open — the response the caller is waiting for is never held hostage by a bookkeeping write
- customer session/device self-service at `GET /v1/auth/sessions`, `POST /v1/auth/sessions/:session_id/revoke`, and `POST /v1/auth/sessions/revoke-all`: Better Auth's session-lifecycle hooks now populate a richer `auth.sessions` mirror (channel, best-effort device label, auth method, timestamps) alongside the audit trail they already fed, and revoking a listed session calls Better Auth's own `revoke-session` endpoint server-side rather than exposing the raw session token to the client. `revoke-all` covers every Better Auth session via Better Auth's own bulk `revoke-sessions` endpoint (which fires the same per-session hooks a single revoke does, so the audit trail and mirror stay correct for free) and, matching "log out everywhere" conventions elsewhere, also ends the caller's own current session rather than leaving a separate "log out everywhere but here" case
- Android native authentication is Better Auth's own session, not a second credential store: the `bearer` plugin (`better-auth/plugins`) accepts the session token via an `Authorization: Bearer <token>` header as an alternative to the session cookie, so `/v1` routes accept either through the same `requireAuthentication` middleware. `crossSubDomainCookies` is enabled whenever `WEBAUTHN_RP_ID` resolves to a real multi-label domain (e.g. `api.vistablox.io`), scoping the session cookie to the shared parent domain (`.vistablox.io`) so a separate frontend subdomain like `admin.vistablox.io` can read the same session.
- Prisma 7 client using the PostgreSQL driver adapter
- all eight documented PostgreSQL schemas and their current tables, relations, indexes, and explicit check constraints
- Vitest API and domain tests
- dependency boundary checks for the documented `router -> schema -> application service -> domain policy -> repository` layering

Customer TOTP (enrolment, verification and the stored secrets), its backup codes and the customer recovery codes were all removed at the device-bound auth cutover; staff never used TOTP. That leaves a known gap: `requireFreshAuthentication` still gates reservation creation and reconfirmation (`POST /v1/offerings/:offering_id/reservations` and `.../reconfirm`), and no customer factor sets `lastFreshAuthAt` any more, so those routes answer `authentication.fresh_auth_required` until device-key step-up signing (or an interim rule) lands. The idempotency middleware is similarly unmounted: AD-057's high-risk-write list (reservation funding, refunds, payouts, and the like) is entirely unbuilt money-movement/settlement work, so there is no current endpoint that legitimately qualifies for it yet — the mechanism is ready for whichever endpoint reaches that list first. Rate-limit tier thresholds are a fixed starting point (`src/shared/http/rate-limit.ts`) pending real production traffic to tune against, per `RATE_LIMITING.md`. Better Auth login success/failure, session creation/revocation, password change/reset, WebAuthn outcomes, staff invitation actions, recovery, and offboarding now feed the unified audit trail. A WebAuthn-verified administrator provisions staff and partner identities through single-use email invitations; after acceptance, the invited user signs in and enrolls a first WebAuthn credential, while adding any later credential requires an already WebAuthn-verified session. That same protected admin surface can initiate a staff reset, immediately offboard an identity, list the full staff/partner roster with role history, grant an existing staff member an additional role, or revoke a single role without a full offboarding; the code intentionally does not enable Better Auth's impersonation-capable admin plugin. Cross-validation of declared identity data and asynchronous webhook processing remain follow-on KYC work. Information-request due dates currently exclude Saturdays and Sundays; a jurisdiction-aware holiday calendar remains future work. The `enqueueTransactionalJob` adapter (a hand-rolled `pg-boss` `IDatabase` implementation forwarding `executeSql` to an existing Prisma transaction's `$queryRawUnsafe`) was confidence-checked against real, unmodified `pg-boss` code — not just this repo's own mocks — using `pg-boss`'s own `pglite` (in-memory Postgres) test adapter as a throwaway, no-network harness; a real job was enqueued and landed correctly in `pgboss.job`. That harness relies on an unexported `pg-boss` internal path and an undeclared transitive dependency, so it was not kept as a committed test, only as a one-time build check — this repo still cannot open a raw TCP connection to Postgres from this environment, so the `Piv`/`Offering`-creation SQL itself is covered by `tests/investor-offering-repository.integration.test.ts`/`tests/origination-offering-handoff.integration.test.ts` (gated on `TEST_DATABASE_URL`, unexercised here) rather than a live run in this session.

Reservation creation (`POST /v1/offerings/:offering_id/reservations`) is now implemented — an atomic, `FOR UPDATE`-locked capacity check (`AD-146`) alongside a Coinbase CDP onramp session created synchronously in the request path (`AD-255`) — and both scheduled jobs the capacity-hold policy needs are also built: `case_timers.reservation_unfunded_expiry` (auto-lapses an unfunded reservation after 15 minutes) and `case_timers.reservation_onramp_poll` (advances `capital_state` past `eurc_purchase_pending`, since Coinbase's onramp/offramp API has no webhook), both run every minute rather than hourly like this worker's other timers. The funding rail is now a deliberate, config-driven switch rather than a hardcoded closed constant — `RESERVATION_FUNDING_RAIL_ENABLED` (default `false`), ANDed in `server.ts` with whether a Coinbase client is actually configured before reaching either service, so the two can never disagree and a half-configured environment can't produce a confusing half-enabled state. It still defaults closed everywhere: this codebase cannot verify Coinbase's live EUR/Base support on its own, the same category of risk that broke the prior onramp design, so every reservation attempt still returns `409 funding_rail_unavailable` until a human completes the live pre-launch checklist in [`docs/investor-offering.md`](docs/investor-offering.md#the-funding-rail-still-stays-closed-by-default) and sets the flag. See also [`docs/coinbase-cdp-onramp.md`](docs/coinbase-cdp-onramp.md).

The reservation lifecycle now has an ending, and it is a three-stage flow rather than one action (`PAYMENT_FLOWS.md`'s "Phase-1 Final Offering Settlement Flow", `AD-214`'s mandatory 168-hour reconfirmation window) — corrected in this codebase after an earlier version collapsed all three stages into one immediate, founder-triggered position creation, which did not match the documented flow. First, `POST /internal/v1/offerings/:offering_id/finalize` (`AD-244`/`AD-245`'s founder "proceed" decision) publishes locked final terms and opens the window: atomically re-verifies `target_raise_eur` is fully collected, the offering is still `pre_offering`, and terms aren't already published, under the same row-lock discipline `createReservation` already uses, then per-reservation either moves a funded one to `reservation_stage: "awaiting_reconfirmation"` or a never-paid one to `"cancelled"` — `offerings.status` deliberately stays `pre_offering`. Second, each investor calls `POST /v1/offerings/:offering_id/reservations/:reservation_id/reconfirm` during the open window to move their own reservation to `"reconfirmed"`; PAYMENT_FLOWS.md's "Silence rule" means no action by expiry lapses it instead. Third, once the window closes, the hourly `case_timers.offering_reconfirmation_window_close` job is the only path in this codebase that creates `settlement.position_ledger` rows — one per reservation that was actually `"reconfirmed"` (moved to `"finalized"`); any reservation still only `"awaiting_reconfirmation"` moves to `"lapsed"` instead, and only now does `offerings.status` become `final_offering`. A funded position's `unit_count`/`cost_basis_eur` both equal the reservation's committed `amount_eur` — a deliberate 1-unit-per-EUR convention, since no total-supply or price-per-unit model exists anywhere in the documented architecture (confirmed by search: `AD-238` names "approved total units" as an explicit, still-open schema gap, and that gap is untouched by this — it applies only to real investor cash reservations, not the owner's non-cash retained position). Stays off-chain and unminted throughout (`position_status: "pending_internal_settlement"`, matching `AD-234`'s dormant chain-settlement gate and `AD-247`'s "only the PIV mints"). See [`docs/investor-offering.md`](docs/investor-offering.md#offering-finalization-is-a-three-stage-flow-not-one-action).

The reconfirmation window's material-change trigger is now built too: `POST /internal/v1/offerings/:offering_id/materiality-records` (`AD-038`) lets staff classify a post-publication change as `per_se_material`, `reviewed_material`, or `non_material`. A material classification (either category) atomically moves every already-`"reconfirmed"` reservation on the offering back to `"awaiting_reconfirmation"` and restarts the full 168-hour window from the classification instant, rather than merely extending it — `non_material` changes are logged only. Every record also surfaces immediately in the investor-facing `change_log`, since that read path already existed. Deliberately not built: `AD-038` also says a per-se-material change "always resets the disclosure pack" — republishing one is a separate manual staff action (see below), not an automatic side effect of classifying. See [`docs/investor-offering.md`](docs/investor-offering.md#materiality-classification-and-the-change-log).

Disclosure-pack authoring itself is now built too, and it turned out to be a bigger gap than just `AD-038`'s reset case: before this, nothing in this codebase could create a `disclosure_packs` row at all — the MinIO document store only ever had a read method, and `hasDisclosurePack`/`disclosure_pack_unavailable` already gates *ordinary* reservation creation, well before publish or reconfirm. `POST /internal/v1/offerings/:offering_id/disclosure-packs` publishes a new version and supersedes whatever was current, taking `document_type`/`document_ref` pairs in the exact same trusted-reference shape origination's own document intake already uses (no file-upload endpoint exists for any document kind in this codebase; getting a file into storage happens outside this API). `document_type` is a closed enum of the 11 kinds `AD-037` names as the mandatory core pack; `is_core_reading` is computed server-side, never client-supplied. Deliberately one reusable primitive rather than wired into any single flow, since the docs imply at least three separate moments a new version is needed (initial pre-offering, final-locked at publish, materiality-triggered republish). Reports `is_complete` (all 11 mandatory types present) informationally on publish — `AD-037`'s "must be blocked if the pack is incomplete" is enforced downstream instead, in two places, both re-deriving completeness from the offering's current pack under their own row lock rather than trusting a stale value: `publishFinalOfferingTerms` now requires a complete pack before it will open the reconfirmation window at all (`409 offering.finalization_disclosure_pack_incomplete` — `PAYMENT_FLOWS.md` step 4 bundles publishing the package together with that step, so the window must never open against a pack no investor could act on), and `reconfirmReservation` re-checks it defensively for the narrower case of a later republish regressing the pack after publish already succeeded (`409 offering.reconfirmation_disclosure_pack_incomplete`). See [`docs/investor-offering.md`](docs/investor-offering.md#disclosure-pack-authoring).

The reconfirmation window's own reminder job is now built too, closing the last named gap in `AD-214`'s three-stage flow: the hourly `case_timers.offering_reconfirmation_reminders` job (`SendReconfirmationRemindersService`) emails every reservation still `awaiting_reconfirmation` at a `platform.settings`-configurable cadence (`offering.reconfirmation_reminder_interval_hours`, seeded to `48`), the same stakes-asymmetry precedent `AD-213` already set for KYC renewal reminders. Its due-ness rule (`isReconfirmationReminderDue`) deliberately differs from KYC's calendar-date match: a reconfirmation reminder repeats indefinitely until the window closes rather than counting back from one fixed due date, so due-ness is instead derived from time elapsed since the last reminder actually sent (read back from the `audit.audit_log` row each send writes, `offering.reconfirmation_reminder_sent`, rather than a new column) — robust to a missed job tick, since an overdue reminder stays due rather than being silently skipped. See [`docs/investor-offering.md`](docs/investor-offering.md#reconfirmation-window-reminders-scheduled-batch-throughout-the-open-window).

The separate single notification `PAYMENT_FLOWS.md`'s Withdrawal/Cancellation Window table names — the window's `Start event` meaning "final terms are locked, the final package is visible in-app, **and investor notification is sent**" — is now built too, closing the one gap the reminder-job paragraph above used to name. Rather than a synchronous email in the founder's own `/finalize` request, `publishFinalOfferingTerms` durably enqueues `case_timers.offering_reconfirmation_window_opened` inside the same transaction that moves reservations to `awaiting_reconfirmation` — the same `AD-145` cross-domain-handoff discipline `case_timers.pre_offering_open_handoff` already established, carrying only the offering ID and the reservation IDs that just moved, never enqueued at all when none did. The consumer (`NotifyReconfirmationWindowOpenedService`) re-resolves each reservation's contact email and window end from the database rather than trusting the payload, and is itself replay-safe the same way the reminder job is: whether a notification was already sent is derived from its own `audit.audit_log` row (`offering.reconfirmation_window_opened_notification_sent`) rather than trusted state, so a pg-boss retry or duplicate delivery can never double-email an investor. See [`docs/investor-offering.md`](docs/investor-offering.md#reconfirmation-window-reminders-scheduled-batch-throughout-the-open-window).

The Didit webhook handler is now durable and asynchronous too, closing the one architectural-rule violation named as follow-on KYC work: `ASYNC_JOBS.md`'s Webhook Handling Rule ("provider callbacks must not run heavy business orchestration inline in the HTTP request... workers, not synchronous HTTP request handlers, own the heavy business processing") was, until now, violated by exactly one endpoint in this codebase — `POST /webhooks/didit` used to verify, correlate, fetch the authoritative decision from Didit (a live outbound call, up to 5 seconds), and apply eligibility policy all inline, before ever acknowledging Didit. `ReceiveDiditWebhookService` (`kyc.router.ts`'s handler) now does only the fast half — verify the signature, validate the shape, durably enqueue the body to `provider_events.didit_webhook` (finally implementing the queue `ASYNC_JOBS.md`'s own Logical Queue Set table had named but nothing had ever created), and acknowledge — while `ProcessDiditWebhookService`, unchanged in its own logic, becomes that queue's worker-side consumer. No separate receipt table was needed: pg-boss's own durable job row, holding the verified body, already is the receipt the async rule calls for. `PrismaKycRepository` now takes a `PgBoss` instance (mirroring `PrismaOfferingRepository`/`PrismaOriginationRepository`'s existing shape), and `worker.ts` gained its own Redis protected-profile-cache wiring so the display-name cache invalidation that used to run inline in the webhook request keeps working unchanged, now from the worker instead. Also fixed in passing, discovered while tracing the session-start lifecycle for this: `StartKycSessionService` used to leave a session's row stuck at `kyc_session_creating` forever — permanently unretryable — if persisting its completion failed after a live Didit session was already created, since nothing called `failSessionStart` to unblock it; it now does. See [`docs/didit-kyc.md`](docs/didit-kyc.md#follow-on-work).

The rest of that same stuck-session gap is closed too, with two hourly `maintenance` jobs. `maintenance.kyc_stuck_session_expiry` (`ExpireStuckSessionCreationsService`) fails outright anything still at `kyc_session_creating`/`creating` past 15 minutes — nothing user-facing happens in that window normally, so anything longer is a crash, and there's no live Didit reference recorded yet to reconcile against regardless. `maintenance.kyc_stuck_session_reconciliation` (`ReconcileStuckOpenSessionsService`, registered only when Didit is configured) handles the harder case: a `kyc_session_open`/`in_progress` row past an hour is re-queried against Didit's own decision endpoint, and — only once Didit reports something other than still-pending — applied through the exact same `evaluateDiditOutcome`/`evaluateProofOfAddressOutcome` + `applyProviderOutcome`/`applyProofOfAddressOutcome` pipeline the webhook path itself uses. No new policy: this only makes sure VistaBlox eventually learns what Didit already knows, for the case its own `Expired`/`Abandoned` webhook was lost in transit or never sent (the only other mechanism that ever advances that substatus). Deliberately not built: alerting on either job — `AD-089`'s `pg-boss` alert scoping covers only the `money_ops`/`settlement` queues, not `maintenance`.

Identity cross-validation — the other named follow-on-work item — was researched rather than built, and the conclusion is worth stating plainly: it isn't a "not ready yet" gap, it's two engineering mistakes waiting to happen. Didit's own live API docs confirm `id_verifications` exposes `nationality`/`issuing_state` (citizenship/document-issuance concepts), not residence — comparing them against declared residence would systematically flag exactly the normal case of an EU/EEA citizen living in a different EU/EEA country than their passport, for a real share of this platform's own target customers, not as a rare edge case. Extending the existing proof-of-address-vs-residence check (`proof-of-address-policy.ts`) to also validate tax residence looked safe at first but isn't either: a single proof-of-address document can only attest to one country, and this codebase's own intake form allows a customer to legitimately declare a different tax-residence country than residence — anyone who did would fail such a check every time, regardless of their proof-of-address's legitimacy. Neither is buildable without either an explicit false-positive-tolerance policy call or new evidence this system doesn't collect (a tax-residency self-certification, out of scope for a Didit identity check). See [`docs/didit-kyc.md`](docs/didit-kyc.md#follow-on-work) for the full reasoning.

Customer full-lockout account recovery (`ACCOUNT_RECOVERY_POLICY.md`) is now built too — the one gap the KYC/auth work above had left as schema-only (`account_recovery_cases` existed since the initial migration; nothing wrote to it). An `admin_operations` staff member opens a case (revokes every existing session first, then restricts the account — `recovery_review` already blocks all authenticated requests, so nothing else needs a new gate), creates a fresh Didit verification session tied to the case, and a **different** second reviewer independently approves or rejects it — enforced atomically in the repository, not just checked in the service, so two reviewers can never be the same person even under a race. Completion sends the customer better-auth's own password-reset link (the same mechanism staff recovery already uses, which is why this path requires email/password to have been one of the two linked methods — `AD-015` already guarantees that for any financially active account), restores normal access immediately, and applies a 72-hour cooldown via a new `recovery_cooldown_active` reservation blocker rather than re-restricting the whole account. Deliberately not built: freezing login-method changes during that cooldown (better-auth's own linking endpoints have no VistaBlox-side policy gate in front of them to extend) and payout/redemption freezing (neither has an endpoint yet). See [`docs/account-recovery.md`](docs/account-recovery.md) for the full flow and what the cooldown does and does not cover.

## Local setup

Requirements: Node.js 22+, npm, and PostgreSQL 18 (local development and testing both use a local instance — `AD-257` — rather than the project's Neon database, which is production-only).

```bash
createdb -U prisma vistablox   # or point DATABASE_URL/DIRECT_DATABASE_URL at your own local instance
cp .env.example .env
npm install
npm run db:generate
npm run db:migrate:dev
npm run dev
```

`DATABASE_URL` is the runtime connection; `DIRECT_DATABASE_URL` is the connection Prisma migrations use. Locally these are the same local instance (`.env.example`'s defaults already point at one, matching `prisma.config.ts`'s own local-dev fallback role); in production they instead point at Neon's pooled and direct connections respectively (`DEPLOYMENT_TOPOLOGY.md`).

`/v1/kyc` and `/internal/v1/kyc-accounts` are this API's own routes (session/WebAuthn enforcement unchanged), backed directly by identity's own in-process application services — no separate process, no internal HTTP call. Didit configuration (`DIDIT_API_KEY`/`DIDIT_WORKFLOW_ID`/`DIDIT_CALLBACK_URL`/`DIDIT_WEBHOOK_SECRET`/`DIDIT_APPLICATION_ID`/`DIDIT_ENVIRONMENT`) is required outright — there's no disabled mode, since account recovery's own re-verification flow reuses the same baseline workflow every other KYC session does. The short-lived Didit-verified display-name cache `GET /v1/investor-profile` reads (`PROFILE_CACHE_URL`, optional) lives here directly too; the profile route remains available and omits names whenever it's unset or a live Didit lookup fails, for any reason. `/webhooks/didit` is this API's own route as well, receiving, verifying, and durably enqueuing the provider's webhook — see the worker paragraph below for who actually processes it. Origination, offering, and investor-profile read `KycEligibility` through a shared `PrismaKycRepository` instance, the same live Prisma query `/v1/kyc` itself reads through — no local projection, no pub/sub. See [`docs/didit-kyc.md`](docs/didit-kyc.md) and [`docs/investor-profile.md`](docs/investor-profile.md) for the reviewed workflows and data boundaries.

`RATE_LIMIT_CACHE_URL` enables the general-purpose `/v1` rate limiter (it may point at the same Redis/Valkey deployment `PROFILE_CACHE_URL` uses); requests are allowed through unmetered, not blocked, while it is absent or unavailable.

The scheduled-job worker is a separate process from the API and must be started alongside it: `npm run worker:dev` locally, `npm run worker:start` against a build. Beyond `case_timers`, it also runs the KYC renewal-reminder timer, stuck-session expiry/reconciliation (`RunKycRenewalTimerService`/`ExpireStuckSessionCreationsService`/`ReconcileStuckOpenSessionsService`), and the Didit webhook's own consumer (`ProcessDiditWebhookService`) — the API's `POST /webhooks/didit` only verifies, durably enqueues, and acknowledges; the worker does the actual policy evaluation and persists the outcome. It shares `DATABASE_URL`/SMTP/Didit configuration with the API, including its own `PROFILE_CACHE_URL` connection, so the display-name cache invalidation a KYC state change triggers keeps working regardless of whether the change arrived through session creation (API) or a webhook (worker). It loads the same environment schema as the API, so it requires but never uses `BETTER_AUTH_SECRET`, but needs no worker-specific environment variables of its own. See [`docs/didit-kyc.md`](docs/didit-kyc.md) for the full request flow.

## Docker

`docker-compose.yml` runs the whole stack — Postgres, Valkey, the API, and the worker — plus Mailpit as a local SMTP catcher (`http://localhost:8025`) so `docker compose up` works without real SMTP credentials.

```bash
cp .env.example .env
# fill in BETTER_AUTH_SECRET at minimum
docker compose up --build
```

The API is then at `http://localhost:3000`. `migrate` is a one-shot service that runs `prisma migrate deploy` before `api`/`worker` start (`depends_on: condition: service_completed_successfully`); rerunning `docker compose up` re-runs it, which is a no-op once migrations are already applied.

`Dockerfile` is a multi-stage build with four targets (`docker build --target=<name>`): `deps`/`build` are intermediate stages that install full dependencies and compile (`prisma generate` then `tsc`); `migrate` reuses `build` since `prisma migrate deploy` needs the Prisma CLI (a devDependency) and the schema/migrations directory; `api` and `worker` copy only the compiled `dist/` output and production dependencies onto a fresh base — neither carries the Prisma CLI, TypeScript, or the schema/migrations directory; `@prisma/adapter-pg` (no native query-engine binary) means plain `node:22-alpine` works with no glibc/OpenSSL matching concerns.

Compose overrides `DATABASE_URL`, `PROFILE_CACHE_URL`, `RATE_LIMIT_CACHE_URL`, `BETTER_AUTH_URL`, and the `SMTP_*` values to point at the container network (`postgres`, `cache`, `mailpit`) regardless of what `.env` has for them; everything else — `BETTER_AUTH_SECRET`, the required `DIDIT_*` values, and the optional `GOOGLE_*` toggles — comes from `.env` via `env_file`.

This has been validated end to end in a real Docker daemon: a clean build of all four targets, `prisma migrate deploy` applying the repository's entire migration history against a fresh Postgres container, and the running `api`/`worker` containers passing their healthchecks and serving real requests against the containerized Postgres/Redis. One real finding from that run, now fixed: Postgres 18's official image expects its volume mounted at `/var/lib/postgresql`, not `/var/lib/postgresql/data` (`docker-compose.yml` already reflects this); pg-boss v12 requires a queue to exist (`createQueue`, idempotent) before it can be scheduled or worked, which `worker.ts` now does on every start.

## Testing

```bash
npm test               # unit tests always run; integration tests skip themselves without TEST_DATABASE_URL
npm run test:watch     # same, in watch mode
```

`npm test` (`vitest run`) is safe to run with nothing else set up: unit tests always execute, and every `tests/*.integration.test.ts` file individually skips itself (`describe.skipIf(process.env.TEST_DATABASE_URL === undefined)`) rather than failing when that variable is absent — there's no separate command for "just the unit tests."

To also exercise the integration suite, point `TEST_DATABASE_URL` at a local PostgreSQL 18 database — a separate one from `DATABASE_URL`, since these tests create and delete real rows and shouldn't touch whatever you're looking at interactively in the dev database:

```bash
createdb -U prisma vistablox_test
DIRECT_DATABASE_URL=postgresql://prisma:prisma@localhost:5432/vistablox_test npm run db:migrate:deploy
TEST_DATABASE_URL=postgresql://prisma:prisma@localhost:5432/vistablox_test npm test
```

This mirrors `.env.example`'s local-dev defaults; `docker compose up -d postgres` (this repo's own `postgres:18-alpine` service) works the same way using that service's `vistablox`/`vistablox` credentials instead. Local PostgreSQL 18 for both development and testing — rather than the design docs' earlier disposable-Neon-branch plan for integration tests, which turned out to never have actually been built — is `AD-257`; `vistablox-design-docs`' `INTEGRATION_TESTS.md` has the full pattern reference. Each integration test file owns its own fixture setup and cleanup directly; there's no separate seed script and no separate Vitest config. `npm run check` (below) runs the same way regardless of whether `TEST_DATABASE_URL` is set.

## Commands

```bash
npm run check          # type checking, architecture rules, tests
npm run build          # production TypeScript build
npm run worker:dev     # run the pg-boss scheduled-job worker and Didit webhook consumer (watch mode)
npm run worker:start   # run the built worker
npm run db:validate    # validate Prisma schema
npm run db:generate    # regenerate Prisma client
npm run db:migrate:dev # create/apply a development migration
npm run db:migrate:deploy
docker compose up --build   # run the full stack (Postgres, Valkey, API, worker, Mailpit) in containers
docker compose down -v      # stop and remove containers + the Postgres volume
```

## HTTP surface currently available

| Method | Path | Purpose |
| --- | --- | --- |
| `GET` | `/health/live` | Process liveness; does not query dependencies |
| `GET` | `/health/ready` | Readiness; verifies PostgreSQL connectivity |
| `POST` | `/webhooks/didit` | Authenticate and durably enqueue Didit status/data webhooks for the worker to process |
| `GET/POST` | `/api/auth/*` | Better Auth browser authentication endpoints |
| `POST` | `/internal/v1/auth/webauthn/registration/options` | Begin staff credential enrollment |
| `POST` | `/internal/v1/auth/webauthn/registration/verify` | Verify and store a staff credential; marks the session MFA-complete |
| `POST` | `/internal/v1/auth/webauthn/authentication/options` | Begin mandatory staff session MFA |
| `POST` | `/internal/v1/auth/webauthn/authentication/verify` | Verify a staff assertion and mark the session MFA-complete |
| `POST` | `/internal/v1/auth/staff-invitations` | Issue a role-scoped staff/partner invitation; requires admin role and WebAuthn |
| `POST` | `/v1/auth/staff-invitations/accept` | Accept an emailed invitation and set the initial password |
| `POST` | `/internal/v1/auth/staff-accounts/:account_id/recovery` | Revoke staff sessions/WebAuthn credentials and email a password reset; requires another admin and WebAuthn |
| `POST` | `/internal/v1/auth/staff-accounts/:account_id/offboard` | Disable staff login and revoke sessions, roles, and credentials; requires another admin and WebAuthn |
| `GET` | `/internal/v1/auth/staff-accounts` | List every staff/partner account and its full role history (active and revoked); requires admin role and WebAuthn |
| `POST` | `/internal/v1/auth/staff-accounts/:account_id/roles` | Grant an additional role to an already-onboarded staff account; requires admin role and WebAuthn |
| `POST` | `/internal/v1/auth/staff-accounts/:account_id/roles/:assignment_id/revoke` | Revoke one role assignment without a full offboarding; requires admin role and WebAuthn |
| `POST` | `/internal/v1/account-recovery-cases` | Open a customer full-lockout recovery case: revokes existing sessions and restricts the account; requires admin role and WebAuthn |
| `GET` | `/internal/v1/account-recovery-cases/:case_id` | Reviewer detail: case status, live re-queried Didit verification status, and safe corroboration facts; requires admin role and WebAuthn |
| `POST` | `/internal/v1/account-recovery-cases/:case_id/didit-sessions` | Create a fresh Didit verification session tied to the case; requires admin role and WebAuthn |
| `POST` | `/internal/v1/account-recovery-cases/:case_id/primary-review` | Record the first reviewer's evidence collection; requires admin role and WebAuthn |
| `POST` | `/internal/v1/account-recovery-cases/:case_id/decision` | A second, different reviewer approves or rejects the case; requires admin role and WebAuthn |
| `POST` | `/internal/v1/account-recovery-cases/:case_id/complete` | Send the recovery completion email and restore access with a 72-hour cooldown on an approved case; requires admin role and WebAuthn |
| `GET` | `/v1/offerings?limit=20&after=...` | Cursor-paginated public offering teasers |
| `GET` | `/v1/offerings/:offering_id` | Authenticated customer-only full offering detail, disclosure metadata, progress, and reservation-readiness blockers |
| `GET` | `/v1/offerings/:offering_id/documents/:document_id/download` | Stream an authorized current or reservation-linked historical disclosure document from private storage |
| `POST` | `/v1/offerings/:offering_id/reservations` | Create a capacity-holding reservation and a Coinbase CDP onramp session (AD-146, AD-255); returns `409 funding_rail_unavailable` until the funding rail is deliberately enabled |
| `POST` | `/internal/v1/offerings/:offering_id/finalize` | Founder "proceed" decision (AD-244/AD-245): publishes locked final terms and opens the 168-hour reconfirmation window (AD-214); requires admin role and WebAuthn |
| `POST` | `/v1/offerings/:offering_id/reservations/:reservation_id/reconfirm` | Investor's own explicit reconfirmation of a reservation during the open window; customer-only |
| `POST` | `/internal/v1/offerings/:offering_id/materiality-records` | Staff materiality classification of a post-publication change (AD-038); a material classification resets already-reconfirmed reservations and restarts the 168-hour window; requires admin role and WebAuthn |
| `POST` | `/internal/v1/offerings/:offering_id/disclosure-packs` | Staff publishes a new disclosure-pack version, superseding the prior current one (AD-037); requires admin role and WebAuthn |
| `GET` | `/v1/investor-profile` | Read the authenticated customer's cross-domain investor profile aggregate |
| `GET` | `/v1/investor-profile/reservations?limit=20&after=...` | Read account-scoped reservation history with the latest capital state |
| `GET` | `/v1/investor-profile/positions?limit=20&after=...` | Read active and internally-settling portfolio positions |
| `POST` | `/v1/investor-profile/wallet` | Register a customer wallet address (KYC-eligible, idempotent by account); requires authentication |
| `GET` | `/v1/auth/sessions` | List the authenticated customer's own sessions (web and native) |
| `POST` | `/v1/auth/sessions/:session_id/revoke` | Revoke one of the authenticated customer's own sessions |
| `POST` | `/v1/auth/sessions/revoke-all` | Revoke every one of the authenticated customer's own sessions, including the current one |
| `GET` | `/v1/kyc` | Read the authenticated customer's local eligibility status and renewal dates |
| `POST` | `/v1/kyc/sessions` | Create one hosted Didit individual-KYC session for an eligible customer account |
| `POST` | `/v1/kyc/proof-of-address/sessions` | Create an owner-only hosted Didit address-verification session after baseline KYC |
| `GET` | `/internal/v1/kyc-accounts/:account_id` | WebAuthn-protected founder/operations read of one account's operational KYC eligibility record |
| `POST` | `/v1/origination-cases` | Create an authenticated, eligibility-gated draft owner intake |
| `GET` | `/v1/origination-cases?limit=20&after=...` | List the authenticated owner's cases |
| `GET` | `/v1/origination-cases/:case_id` | Read one owner-scoped case; out-of-scope IDs return `404` |
| `POST` | `/v1/origination-cases/:case_id/submit` | Submit a draft as immutable revision 1 with required evidence |
| `GET` | `/v1/origination-cases/:case_id/information-requests` | List the owner's published request history |
| `POST` | `/v1/origination-cases/:case_id/information-requests/:request_id/respond` | Answer the current request with immutable revision N+1 |
| `GET` | `/v1/origination-cases/:case_id/messages` | Read the owner's own case's `applicant`-lane discussion thread |
| `POST` | `/v1/origination-cases/:case_id/messages` | Post to the `applicant` lane on the owner's own case |
| `GET` | `/internal/v1/origination-cases?stage=submitted&limit=20&after=...` | WebAuthn-protected founder operations case queue |
| `GET` | `/internal/v1/origination-cases/:case_id` | Founder review detail, current revision, evidence, and request history |
| `POST` | `/internal/v1/origination-cases/:case_id/information-requests` | Publish an owner request and start the configured response window |
| `POST` | `/internal/v1/origination-cases/:case_id/decisions` | Approve with IPO terms or reject a submitted case |
| `POST` | `/internal/v1/origination-cases/:case_id/close` | Withdraw (any pre-terminal stage) or late-stage reject (`pre_offering_open` only) a case |
| `GET` | `/internal/v1/origination-cases/:case_id/messages?lane=internal_case\|applicant` | Read either discussion lane on any case |
| `POST` | `/internal/v1/origination-cases/:case_id/messages` | Post to either lane on any case, as the founder |

All errors follow the documented envelope: `type`, `code`, `title`, `status`, `detail`, `trace_id`, and optional `field_errors`.

## Database notes

The Prisma schema lives under `prisma/schema/` (a multi-file schema: `schema.prisma` for the datasource/generator and most models, `auth.prisma` for auth's own tables in their own `auth` schema namespace); reviewed SQL migrations are under `prisma/migrations`. Check constraints and append-only revision enforcement that Prisma cannot express are maintained directly in migration SQL. Better Auth owns its four `public.auth_*` tables through its PostgreSQL adapter — product code deliberately does not model or query them through Prisma. The runtime uses the generated client in `src/generated/prisma`, which is deliberately excluded from Git and regenerated during setup/build.
